extends _partial/layout
block site_title 
    != '夭夜的博客'
block content
    .blogcontent
        .posts
            - page.posts.each(function (item) {
                include postUtils
                +make_post(item,false)
            - })
            include mixins
            +_paginator()
        .rightpane.fadeIn
            include _partial/sidebar
        script.
            filter()
            //- - var p = paginator(total:site.posts.length,prev_text:"上一页",next_text:"下一页",prev_next:false)
            //- - console.log(p)
            